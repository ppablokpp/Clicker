# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(Downgraded from SDK 57 to SDK 54 — the App Store build of Expo Go only
supports SDK 54 as of testing; see the project's plan file for details.)
