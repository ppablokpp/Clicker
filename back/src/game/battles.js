// Fixed for now, same spirit as every other "one starter value, tune later"
// constant this session (Reactor, Multidisparo's base cost, etc.).
export const BATTLE_WAGER = 10_000
export const BATTLE_DURATION_SECONDS = 30
